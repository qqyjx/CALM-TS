# CALM-TS runtime dependencies
# Tested on Python 3.11 (Linux x86_64); A800 reference env: kdd26-camera (conda)
# Reference versions are the camera-ready re-run snapshot. Newer minor versions
# of these libraries should remain compatible.

numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
openai>=1.30
xgboost>=2.0

# PhysioNet Challenge 2015 raw read (preprocess_physionet.py only)
wfdb>=4.1

# Optional: MIMIC-III preprocess (preprocess_mimic.py with --format parquet)
# pyarrow>=14.0
# pandas>=2.0

